# RSI / Brain Analytics Dashboard

Static HTML dashboard for OpenCrabs RSI (Recursive Self-Improvement) and brain file analytics. Generated by Python script, served via Telegram or paste services.

<img width="1398" height="741" alt="image" src="https://github.com/user-attachments/assets/dc620377-5ca7-4db6-9cd8-4a558242698e" />
<img width="1382" height="892" alt="image" src="https://github.com/user-attachments/assets/959c0d67-dde7-4915-944c-3c5e0a474083" />


## Features

- **Brain file sizes** — KB + lines, with time-series history from `.bak` files
- **Tool usage stats** — most/least used, failure rates, stacked success/failure charts
- **RSI tracking** — applied improvements per tool, mention rates, cumulative timeline
- **Scatter plot** — usage vs failure rate, bubble size = RSI applied, color scale
- **Profile switcher** — ops / redevest profiles in one page
- **Expandable charts** — click any chart header to toggle full-width

## Usage

```bash
# Generate dashboard
python3 ~/.opencrabs/analytics/gen_dashboard.py

# Output: ~/.opencrabs/analytics/index.html
```

## Cron (manual setup)

The cron job is **not** created automatically. You need to set it up yourself using OpenCrabs cron. The recommended setup:

1. Generate the dashboard: `python3 gen_dashboard.py`
2. Upload `index.html` to a paste service (e.g. `litterbox.catbox.moe`)
3. Send the link to your Telegram/chat

Example cron prompt:
```
Run python3 ~/.opencrabs/analytics/gen_dashboard.py to generate the dashboard,
then upload ~/.opencrabs/analytics/index.html to litterbox.catbox.moe via:
curl -F 'reqtype=fileupload' -F 'time=72h' -F 'fileToUpload=@/root/.opencrabs/analytics/index.html' https://litterbox.catbox.moe/resources/internals/api.php
Then reply with ONLY the URL.
```

## Data Sources

- Brain files: `~/.opencrabs/profiles/{profile}/`
- SQLite: `~/.opencrabs/opencrabs.db` (tool_executions, feedback_ledger)
- RSI logs: `~/.opencrabs/rsi/improvements.md` + `history/*.md`

## Dependencies

- Python 3 (no pip packages — stdlib only)
- Chart.js + chartjs-adapter-date-fns via CDN (in generated HTML)
